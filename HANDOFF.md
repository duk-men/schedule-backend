# 근무표 배정 알고리즘 개선 인수인계

## 0. 이 문서의 목적

React 단일 파일로 된 매장 근무표 앱(`ScheduleDemo`)의 자동 배정 알고리즘을 개선한다.
별도 세션에서 동일 제약을 CP-SAT(OR-Tools)로 모델링해 **검증을 마친 사실**들이 있고,
그 과정에서 **기존 JS 코드의 실제 버그 3건**이 드러났다. 아래는 그 결과다.

참고 구현: `roster.py` (Python, OR-Tools CP-SAT). 제약과 목적함수의 정답 명세로 취급할 것.
단, 이 파일을 프로젝트에 그대로 넣으라는 뜻은 아니다. 6절의 단계 지시를 따를 것.

---

## 1. 문제의 정체

- 정식 명칭: **Nurse Rostering Problem (NRP) / Shift Scheduling Problem**. NP-hard.
- knapsack / 단순 DP / BFS로는 정확히 풀리지 않는다.
- 현재 코드의 위치: **Multi-start Randomized Greedy** (`AUTO_TRIES = 2500`).
  시드만 바꿔 매번 **처음부터 다시 짜는** 방식이라, "거의 다 좋은데 한 칸만 나쁜 해"를
  국소적으로 고칠 수단이 전혀 없다. 이게 품질 한계의 근본 원인이다.

---

## 2. 검증된 사실 (CP-SAT 실행 결과)

신중동점 기본 데이터, 2026-08-10 ~ 08-16 (월~일) 1주 기준.

- **최적해 존재. 커버리지 부족 0분 / 바 인원 미달 0 / 초과근무 0건.** 2.9초, OPTIMAL 증명.
- 즉 greedy 2500회가 찾지 못하던 무결점 해가 실제로 존재한다. **개선 여지가 있음이 확정됐다.**
- 남은 페널티는 배정서(주 최대 4)·조은솔(야간 1인) 상한에서 오는 구조적 형평 차이뿐이며
  더 줄일 수 없다.

이 결과는 개선 작업의 **성공 기준 상한선**으로 쓸 것. 같은 입력에서 부족 0분이 안 나오면
아직 개선 여지가 남은 것이다.

---

## 3. 발견된 버그 (우선순위 순)

### 버그 1 — 주 근무 상한이 실제로 작동하지 않음 (심각)

`wcOf`는 쩜오를 0.5로 가중한 **근무일수 합계**만 센다. 출근 횟수는 아무도 세지 않는다.

```js
// autoAssignAll > pool
.filter((e) => wcOf(e.id, date) + unit <= Math.min(e.maxPerWeek + capBonus, WEEK_CAP))
```

결과: 주 최대 4인 직원이 쩜오만 쓰면 **주 8회 출근**이 가능하다.
`WEEK_CAP = 6` ("어떤 경우에도 주 6일을 넘길 수 없다")도 같은 단위라 **보장이 안 된다.**
실제로 CP-SAT 초기 모델이 이 규칙을 그대로 옮겼더니 배정서가 주 6회 출근하는 표가 나왔다.

영향 범위 (전부 같은 결함 공유):
- `autoAssignAll > pool` 의 상한 필터
- `autoAssignAll > placeAnywhere` 첫 줄 가드
- `bump` (집계 자체)
- UI `weekDaysOf` / `storeDaysOf` → 주간 요약의 `{n}/{cap}` 표시
- UI `weekLoad` → 직원 고르기 시트의 `주 {load}/{cap}` 및 `overWeek` 판정

### 버그 2 — 휴게 배치가 고정 규칙이라 바 인원 미달을 스스로 만든다

`breakPlan`은 "오픈조 12:30부터, 마감조 15:30부터, 출근 이른 순서대로 1시간씩"이라는
**절차적 상수 규칙**이다. 배정 결과와 무관하게 정해지므로, 그 결과가 `floorCurve` →
`floorGap` → `scoreBoard` 페널티로 흘러들어가는데 정작 알고리즘은 이걸 **조정할 수단이 없다.**

CP-SAT에서 휴게 시각을 결정변수로 풀어주자(출근 2시간 후 ~ 퇴근 1시간 전 창, 동시 1명)
바 인원 미달이 전 요일 0이 됐다. 앱에서 `floorMinOf < FLOOR_MIN` 경고가 자주 뜨는 주된 원인.

### 버그 3 — `scoreBoard`가 슬롯 충족이 아닌 시간 커버리지만 본다

`demandFor`는 슬롯별 필요 인원을 시간축 곡선으로 합산한 뒤 그 곡선만 비교한다.
그래서 "찐오(8-18) 자리는 비었지만 이른오전(8-15)+미들(10-20)이 그 시간을 덮었다"면
**부족 0분으로 채점된다.** 그런데 월간 뷰는 슬롯별 막대를 그리므로 사용자에겐 "찐오 빈칸"으로
보인다. 채점 기준과 표시 기준이 어긋나 있다.

**이건 버그인지 의도인지 확인이 필요하다.** 사용자에게 물을 것:
필수 슬롯(찐오/마감/13)은 시간 커버리지와 무관하게 반드시 그 슬롯 자체를 채워야 하는가?

---

## 4. 확정된 규칙 재정의 (사용자 승인 완료)

| 항목 | 확정 내용 |
|---|---|
| `maxPerWeek`의 의미 | **출근 횟수**. 쩜오든 풀타임이든 1회로 센다 |
| 근무일수 | 쩜오 0.5 / 풀타임 1.0. 표시·형평 계산용 |
| 인원 미달 시 초과 | 출근 **+1회**까지, 근무일수 **+1일**까지. 단 주 6회 하드캡 |
| 초과 산정 | `max(근무일수 - 2*cap, 출근횟수 - cap)` 를 0.5일 단위로 (쩜오로 상한을 피해가는 것 차단) |
| 초과 페널티 | 0.5일당 180점 = 평일 3시간 부족 상당. 실제 구멍이 클 때만 쓰이게 |
| 형평 항 | 분산(제곱) 대신 **max-min(range)** 로. 선형이라 훨씬 빠르고 실무 효과는 동일 |

검증: 김규리 상한을 5→3으로 낮춰 돌렸더니, CP-SAT가 풀타임 대신 **쩜오(16-22)를 골라**
출근 4/3회·근무 3.5일·초과 +0.5일로 최소 단위만 넘겼다. 의도대로 동작.

---

## 5. 목적함수 명세 (`scoreBoard`가 따라야 할 형태)

단위는 분(minute). 값이 낮을수록 좋음.

```
커버리지 부족   부족 인원 × 30분 × (금토일 4 / 평일 1)
부족 발생 일수  부족이 있는 날 1일당 20
바 인원 미달    (11:00~17:00, 2명 미만) 부족분 × 30분 × 2
인원 과잉       max(3, 그시각 필요인원) 초과분 × 30분 × 1.5
형평            (최대 근무일수 - 최소 근무일수) × 100   ← 0.5일 단위
주 최소 미달    출근 횟수 미달 1회당 500
초과근무        0.5일당 180
```

---

## 6. 작업 단계 (이 순서로)

### 1단계 — 버그 1 수정 (CP-SAT 도입 여부와 무관하게 필수)

- 목표: 출근 횟수와 근무일수를 **분리 집계**하고, `maxPerWeek`/`WEEK_CAP`을 출근 횟수에 건다.
- 검증: 쩜오만 반복 배정되는 인위적 입력(직원 1명, 상한 4)에서 출근이 4회를 넘지 않을 것.
  현재 코드에서는 8회까지 통과한다 — 수정 전 이 실패를 먼저 재현해 볼 것.
- 주의: `bump`, `pool`, `placeAnywhere`, `weekDaysOf`, `storeDaysOf`, `weekLoad`,
  주간 요약 UI, 직원 고르기 시트까지 전부 손대야 일관된다. 한 곳만 고치면 표시와 판정이 어긋난다.

### 2단계 — 3절 버그 3에 대한 사용자 확답 받기

필수 슬롯을 하드 제약으로 둘지 결정. 이 답에 따라 3단계 목적함수가 갈린다.

### 3단계 — 목적함수를 5절 명세에 맞추고, 개선 루프 도입

greedy를 처음부터 2500회 다시 돌리는 대신, **완성된 해를 국소 개선**하는 층을 얹는다.
권장은 **LNS (Ruin & Recreate)**: 하루치 또는 한 명분만 지우고 기존 `autoAssignAll`로
다시 채우기를 반복. 기존 함수를 재활용할 수 있어 구현량이 가장 적다.

- 검증: 2절의 기본 데이터·기본 주에서 `scoreBoard` 결과가 **부족 0분**에 도달할 것.
  도달 못 하면 아직 남은 문제가 있는 것이다.

### 4단계 (선택) — CP-SAT 백엔드

브라우저에서 OR-Tools를 못 돌리므로 FastAPI 등 별도 서비스가 필요하다.
아키텍처 결정 사항이므로 **사용자 승인 전에는 착수하지 말 것.**
착수한다면 `roster.py`의 모델을 기준으로 삼되, 다매장·야간 지원·못박은 근무(pins)·
종료일(until)·달 단위 범위는 아직 모델에 반영돼 있지 않으니 추가해야 한다.

---

## 7. `roster.py` 모델의 현재 한계 (그대로 믿으면 안 되는 부분)

- 단일 매장(신중동)만. 다매장 동시 배정 없음. 타 지점 야간 인력만 후보에 포함.
- 1주 고정. 달 단위 미검증.
- 주 첫날 00~08시는 **지난주 야간이 채워져 있다고 가정**하고 채점에서 제외했다.
  (앱의 `firstDayBoundaryGap` 경고와 같은 지점)
- `pins`(못박은 근무), `until`(종료일)은 자료구조만 있고 실데이터가 비어 있어 미검증.
- 고정 요일(`fixedDays`)은 하드 제약으로 넣었다. JS는 `placeAnywhere` 최선노력이라 다르다.
  어느 쪽이 맞는지 사용자 확인 필요.

---

## 8. 코딩 규칙 (사용자 지정)

- 수정 위치를 "어디부터 어디까지 교체"로 명시해 복붙 가능하게 제시할 것.
- 기존 주석을 삭제하지 말 것. 새 주석은 구체적으로 달되 이모지 사용 금지.
- 요청 범위 밖 리팩터링·개선 금지. 기존 스타일에 맞출 것.
- 불명확하면 멈추고 물을 것. 선택지를 제시해 답변 범위를 좁힐 것.
